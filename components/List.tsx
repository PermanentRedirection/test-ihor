import * as React from 'react'
import { User } from '../interfaces'

type Props = {
  items: User[]
}

const List = () => (
  <ul>
    New new new
    <div>
      new
      <p>sadas</p>
    </div>
  </ul>
)

export default List
